# FHIP AI Placeholder Register — 2026-09-22

Audited against `origin/main` @ `f0c0895`. Classification per the audit brief §44: **A** = required for 11.1–11.6, missing; **B** = intentional future 11.7–11.10 placeholder; **C** = obsolete/dead; **D** = environment/config gap; **E** = documentation-only stale reference.

| ID | File/path | Line/ref | Description | Phase | Class | User-reachable? | Production-reachable? | Security/cost impact | Required action | Status |
|---|---|---|---|---|---|---|---|---|---|---|
| PH-01 | `lib/ai/providers/openaiProvider.ts` | `generateStructured()`, ~L33-45 | Real OpenAI adapter unconditionally throws `PROVIDER_UNAVAILABLE` regardless of credentials/environment | 11.3 | A | No | No (never called from any live path) | None currently (fails closed); blocks any real Insight Pack output | Dedicated remediation branch, PO-authorized, real credentials, DEV smoke test before any activation | Open |
| PH-02 | `app/api/admin/ai/insight-packs/generate/route.ts` | `resolveProvider()`, ~L29-31 | Only `provider === 'mock'` resolves to a usable provider; any other value throws | 11.3 | A | No | No | None (fails closed, disclosed in comment) | Same as PH-01 — resolve together | Open |
| PH-03 | `lib/ai/insightPack/batchOrchestrator.ts` | whole file | Batch types (`BatchCapableProvider`, `BatchPollResult`) exist; only implementation is `MockInsightPackProvider`; no cron/queue/scheduler anywhere in the directory | 11.3 | A | No | No | None currently; "monthly automatic pack" cannot happen without this | Infra decision (cron vs. worker vs. queue) then build | Open |
| PH-04 | *(does not exist)* | n/a | Module 11.6 — Next Best Action: zero code, zero migration, not stranded on any of ~522 branches searched | 11.6 | A | No | No | None currently; but SQ-AI-003/025's "already-ranked, never re-ranked here" claim has no enforcement module to point to | Dedicated scoping (thresholds must trace to existing certified modules per brief §38) + build, PO-authorized | Open |
| PH-05 | `app/(app)/admin/**` | n/a (absence) | No Admin UI page for any of the 19 `app/api/admin/ai/*` routes; all AI admin controls are API-only | 11.1 / cross-cutting | A | No (admin-only anyway) | Yes, admin-reachable via direct API call today | Low — routes are `requireAdmin()`-gated; risk is operability, not exposure | Build Admin UI screen(s) surfacing existing real data (`buildUsageDashboard()` etc.) | Open |
| PH-06 | `lib/ai/entitlement/capabilities.ts` | L113 | `AI_SCENARIO_NARRATION: false` — Scenario Coach capability, explicitly commented "deferred" | 11.9 | B | No | No | None — correctly disabled | Do not enable; leave as-is until 11.9 is scoped | Correct as-is |
| PH-07 | *(searched, not found)* | n/a | No embeddings/vector/pgvector/similarity-threshold infrastructure anywhere under `lib/ai/` | 11.8 | B | No | No | None — nothing to accidentally enable | None — confirms clean slate for future 11.8 work | Correct as-is |
| PH-08 | *(searched, not found)* | n/a | No open-text/free-form AI chat surface anywhere in the app | 11.7 | B | No | No | None | None | Correct as-is |
| PH-09 | `supabase/migrations/0110_module11_ai_foundation.sql` | `ai_recommendations`, `ai_feedback` table definitions | Tables created but never read/written by any route or service under `app/` or `lib/` (only referenced by an RLS-certification script) | 11.0 foundation | C | No | No | None — dead schema, RLS-enabled | Remove only if confirmed safe/tested, or repurpose when a real consumer is designed | Open (low priority) |
| PH-10 | `.env.example`, `amplify.yml` | n/a (absence) | Module 11's own real-provider env var (e.g. a hypothetical `OPENAI_API_KEY` used by `lib/ai/providers/openaiProvider.ts`) is undocumented — by design, since the adapter is inert. The *unrelated* AIE subsystem's `AIE_OPENAI_API_KEY`/`AIE_AI_MODEL`/`AIE_AI_PROVIDER` **are** documented and forwarded. | 11.3 | D | No | N/A | None now; becomes relevant the moment PH-01/PH-02 are remediated | Add the correct env var names to `.env.example`/`amplify.yml` as part of the same remediation branch as PH-01 | Open (blocked on PH-01) |
| PH-11 | Prior session memory index (`live_recovery_lr_programme.md`, referencing commit `a99b455`) | n/a | Memory characterizes the "E2E Consolidated Certification" as "FULL PASS, 2026-08-27... 29/29 PASS, zero violations. Terminal," but commit `a99b455` is **not an ancestor of `origin/main`** (lives only on unmerged branch `cert/e2e-consolidated-input-integrity`) and its own message says "CONDITIONAL PASS — PRODUCTION PREREQUISITE INCOMPLETE (0093 not yet live in production)" | Cross-cutting (Modules 1-10, not Module 11) | E | N/A | N/A | Reputational/process — a stronger claim than the primary source supports is now embedded in a memory file other audits may cite | Correct the memory entry to reflect the commit's own weaker, more accurate verdict; independently re-verify current state of migration `0093` in production | Open |
| PH-12 | `lib/ai/insightPack/` (whole directory) | n/a (absence) | No dedicated deterministic ranking/scoring builder for the `priority_review_areas` pack block that backs SQ-AI-003/025; ranking provenance traces only as far as a `priority` field on `RecommendationEntry` in the context object, with the actual system-prompt instruction text not read in this pass | 11.3/11.4/11.6 boundary | A (open question, not confirmed defect) | Indirectly (via SQ-AI-003/025 answers, once a real provider exists) | Not yet (blocked on PH-01) | Unknown until resolved — could mean "AI ranks," which would violate §37 once live | Code/prompt review to confirm the prompt instructs preservation of upstream ranking, not re-derivation | Open — resolve before PH-01/PH-04 |
| PH-13 | `app/api/internal/ai/provider/health/route.ts` | L9, L17 | Hardcodes `new MockAIProvider()` unconditionally for the provider health-check endpoint, with a disclosed comment | 11.0 | D (disclosed, but functionally means health-check can never validate a real provider) | No (internal route) | Yes (internal) | None currently | Update once PH-01 is remediated so health-check can reflect real provider status | Open (blocked on PH-01) |

## Summary counts

- Class A (required, missing): 6 (PH-01, PH-02, PH-03, PH-04, PH-05, PH-12)
- Class B (future phase, correctly untouched): 3 (PH-06, PH-07, PH-08)
- Class C (obsolete/dead): 1 (PH-09)
- Class D (config/environment gap): 2 (PH-10, PH-13)
- Class E (documentation-only stale reference): 1 (PH-11)

No item in this register was remediated in this pass (see the Production Certification report §S/§T for why the highest-impact ones — PH-01/02/03/04 — were deliberately left for a PO-authorized dedicated branch rather than improvised here).
