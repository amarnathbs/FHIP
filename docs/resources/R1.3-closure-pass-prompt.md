# FHIP — Claude AI Closure Prompt

## R1.3 Closure Pass — Live Editor Responsive, Accessibility & Final Regression Gate

**Project**: Financial Health Intelligence Platform™ — FHIP
**Module**: Resources / Financial Knowledge & Insights
**Phase**: R1.3 Closure Pass

### Objective

R1.3 — Article, Guide & FHIP Explainer Editors is functionally implemented and database-verified, but is currently treated as:

**CONDITIONAL PASS**

for Product Owner acceptance because the required live-browser UX/accessibility evidence has not yet been completed.

This closure pass has one purpose:

> Convert R1.3 from CONDITIONAL PASS to FULL PASS through genuine live verification of the implemented editor surfaces.

Do not redesign R1.3 unless live testing reveals an actual defect.
Do not begin R1.4.

---

## 1. Current Approved R1.3 Baseline

Treat the current R1.3 implementation as the approved starting point.

Branch: `feature/resources-r1-3-core-editors`

Approved R1.3 commit: `5c59112 feat(resources): R1.3 Article/Guide/FHIP Explainer editors`

Parent R1.2 checkpoint: `c52851f feat(resources): complete R1.2 admin dashboard, content browsing shell, and closure-pass fixes`

R1.3 currently includes: Article creation/editing, Guide creation/editing, FHIP Explainer creation/editing, New Content chooser, structured content blocks, starter templates, taxonomy, metadata, SEO, compliance classification, GREEN/AMBER/RED workflow, revision history, manual Save Draft, autosave, save-status indication, stale-write protection, unsaved-change protection, Admin Preview, sanitisation, RLS/RBAC protections, workflow RPC integration.

Preserve these.

## 2. Database State

Migration: `0037_resources_editor_support.sql` has already been applied to DEV. Do not reapply or alter it unless live verification discovers a genuine database defect.

DEV project: `vqycarelcoijzwlpkpcz`

Production must not be touched.

## 3. Already-Proven Engineering Evidence

Do not unnecessarily repeat design work already proven. Current verified evidence includes: migration 0037 applied to DEV; 19/19 R1.3 live database tests passing; GREEN/AMBER/RED security logic proven; TypeScript clean; R1.3 lint clean; production build passing; structured block/unit tests passing; sanitisation tested; stale-write protection tested; draft visibility/RLS tested; Author cannot publish; Analyst/customer cannot create/edit drafts.

The missing evidence is primarily live rendered-editor behaviour.

## 4. Closure Scope

Test exactly these five surfaces: (1) New Content chooser, (2) Article Editor, (3) Guide Editor, (4) FHIP Explainer Editor, (5) Admin Preview.

At exactly these five viewport widths: 1440, 1280, 1024, 768, 390.

This produces 25 mandatory live responsive cells. All 25 must be genuinely tested in a browser. Code review alone is not sufficient.

## 5. Git / Worktree Safety

Before doing anything: `git status`, `git branch --show-current`, `git log -3 --oneline`.

Confirm R1.3 commit `5c59112` exists. There may be unrelated concurrent work in the main working directory. If the current checkout contains unrelated WIP: Preferred — use an isolated worktree from `feature/resources-r1-3-core-editors` or commit `5c59112`. Do not: reset unrelated files; stash someone else's active work unless clearly safe; switch the main working directory destructively; mix unrelated Phase 1 work into R1.3 closure fixes.

The final report must state the actual branch/worktree used.

## 6. Environment Safety

Before live testing: confirm the application is using `NEXT_PUBLIC_SUPABASE_URL` for DEV: `https://vqycarelcoijzwlpkpcz.supabase.co`. Do not print credentials. Do not query or modify production. If DEV cannot be conclusively established: STOP and report the blocker.

## 7. Authentication for Live Testing

Use a legitimate authorised DEV Resources user. Preferred role: `resource_admin` or Super Admin. If a disposable DEV user is required: create only in DEV; use the project's already-established test-user/session method; create required `user_profiles` state if the application onboarding gate requires it; clearly identify the account as test-only; remove it afterward. Do not use or modify a real customer's account unnecessarily. Do not bypass authentication by weakening middleware or route protection. If an authenticated browser session genuinely cannot be established: do not replace the live tests with code review. Report the exact blocker.

## 8. Existing Review Fixtures

The R1.3 implementation report states three named DEV fixtures were intentionally left available: `R1.3 TEST — Article` (slug `r1-3-test-article`), `R1.3 TEST — Guide` (slug `r1-3-test-guide`), `R1.3 TEST — Explainer` (slug `r1-3-test-explainer`). The intended classifications are: Article — GREEN; Guide — AMBER; Explainer — RED. Do not import the R0-A 218-record Content Master. If these fixtures are missing, create minimal DEV-only replacements.

## 9. Test Surface 1 — New Content

Route: `/admin/resources/content/new`. Live-test at 1440/1280/1024/768/390. Verify: page title visible; explanatory text readable; Article/Guide/FHIP Explainer cards usable; cards reflow properly; no clipping; no overlapping text; no horizontal overflow; focus visible; cards accessible by keyboard; correct descriptions displayed. At 390: verify cards become a clean single-column or appropriate mobile layout.

## 10. New Content Creation Sanity

Using the authenticated authorised user: create one temporary Article draft. Confirm: draft created; redirected to editor; status = Draft; visibility = Private; GREEN default; Global default; Indexable = false; Article starter blocks appear. Delete/clean the temporary draft afterward if appropriate. Do not leave unnecessary fixtures.

## 11-14. Article Editor

Open `R1.3 TEST — Article` or equivalent. Test at all five widths. Verify header (breadcrumb/back, content type, status, title, save status, Save Draft, Preview, workflow action), main content (title/character count/slug/availability/excerpt/character count/structured block area/Add Block/starter-template blocks — no clip/overflow), metadata (Primary Category, Additional Categories, Tags, Jurisdiction, Difficulty, Freshness, Visibility, Featured, Author, Reviewer, Compliance Reviewer, Compliance Classification, SEO Title, Meta Description, Canonical URL, Indexable, Primary CTA, Secondary CTA — sidebar usable at desktop, stacks at 768/390), and long-content wrapping (long title/excerpt/canonical URL/SEO description/category-tag label — no overflow, no hidden controls).

## 15-16. Guide Editor

Open `R1.3 TEST — Guide` or equivalent. Test all five widths. Verify Guide starter template (Overview, Before You Begin, Step 1, Step 2, Step 3, Example, Key Takeaways) are ordinary editable/removable blocks. Verify AMBER behaviour: AMBER badge/label; AMBER explanatory text; compliance-review requirement obvious; inappropriate GREEN-only workflow action absent; Publish not incorrectly available before compliance approval. Do not weaken workflow logic for testing.

## 17-18. FHIP Explainer Editor

Open `R1.3 TEST — Explainer` or equivalent. Test all five widths. Verify starter structure (What it is, Why FHIP measures it, How it is calculated, Example, How to interpret your result, What may improve it, Important limitations) all editable/removable. Verify RED behaviour live: RED badge visible; RED explanatory warning visible; no functional Publish/Schedule option; restricted status communicated in text, not only colour. Attempt a forbidden publication transition only if safely possible through the existing UI/test path. Expected: DENIED. Do not bypass the database for UI convenience.

## 19-20. Admin Preview

Open preview for each representative content type where practical; one Preview surface at each width. Verify Admin Preview indication, title, excerpt, all supported blocks render (headings, lists, callouts, warnings, table if present), inline bold/italic/link rendering, long content wraps, no editing controls appear. Confirm preview is Admin-only: unauthenticated → redirect/login denial; Analyst/customer → cannot see private draft preview (if practical). Do not expose draft preview publicly.

## 21-26. Responsive Matrix and Verification Criteria

25-cell matrix (5 surfaces × 5 widths) required, all PASS/FAIL, no "reviewed in code"/"likely pass" substitutions. A cell passes only if: page loads; no unintended page-level horizontal overflow; no meaningful clipping; all controls remain usable; text remains readable; block controls remain accessible; metadata remains reachable; workflow controls remain reachable; save/preview controls remain usable; no overlapping elements; no inaccessible off-screen content. Desktop (1440/1280): Main Editor | Metadata Sidebar layout verified. 1024 is a critical breakpoint (two-column or stacked, either acceptable if usable). 768: not squeezed into unusable two-column; AppShell drawer works. 390: single-column, no horizontal scroll, all controls usable, sticky header reasonable.

## 27-31. Block Editor Functional Pass

Add Paragraph, H2, Bulleted List, Numbered List, Key Takeaways, Callout, Example, Warning, Quote, Divider (+ Table if implemented) — all render and remain editable. Move Up/Move Down — visible order changes, persists after save/reload. Duplicate — new block gets distinct ID, same content, original remains, persists. Delete — empty block lightweight; populated block requires confirmation dialog (cancel once, verify remains, then confirm, persists). Add Block keyboard accessibility: Tab to it, open via keyboard, navigate to Paragraph/Heading/Key Takeaways, activate one — document any tool limitation honestly rather than claim untested activation.

## 32-37. Accessibility

Block control keyboard accessibility (Move Up/Down/Duplicate/Delete — visible focus, logical order, descriptive names). Full Tab/Shift+Tab walk through Article Editor, no keyboard trap. Focus visibility (no `outline: none` without replacement). Metadata accordion (`<details>/<summary>`) accessibility at 390 and desktop. Field labels for all listed controls, no placeholder-only controls. Validation error accessibility: incomplete draft submission blocked, clear validation summary, field-level errors, `aria-invalid`, errors associated with fields, focus moves sensibly where implemented; fix and retry expected PASS.

## 38-43. Save / Autosave / Preview

Save Draft live functional test (Saving… then Saved only after success, persists on reload). Autosave live functional test (Unsaved changes → Saving… → Saved in correct sequence, no misleading Saved state before success, persists on reload). Autosave failure state safely simulated if practical (Save failed, Retry Save, no false Saved; remove fault after). Unsaved navigation protection (confirmation on Back/All Content, cancel stays, confirm proceeds; no warning after successful save). Browser reload protection where technically exposable. Preview button opens correct saved content, draft stays private, preview doesn't modify content.

## 44-48. Workflow

GREEN: Author submits, Editor approves, Publisher publishes (or safe equivalent) — correct buttons/transitions/history/role restrictions/published timestamp. AMBER: Draft → Editorial Review → Compliance Review; Editor-only approval bypass attempt expected DENIED; Compliance Reviewer/Resource Admin valid approval; compliance fields recorded; then publish as permitted. RED: warning visible, no valid Publish, forbidden transition attempt expected DENIED, do not alter RED DB constraints. Revision History: manual saves, save with Change Summary, workflow submission — incrementing version, timestamp, editor identity, change summary, expected snapshot count; autosave does not flood history. Stale-write UI sanity recommended if practical (may rely on already-proven live DB tests).

## 49-56. Security / State / Evidence

Sanitisation live visual check: `<script>alert('x')</script>` and a `javascript:` link — visible literal/escaped text, no execution, no alert, not rendered as executable link. Large content sanity (20+ blocks) — editor remains responsive, scrolling usable, sticky elements intact, Save/Preview work. Console check — 0 new R1.3 application errors, separate known dev/HMR noise. Network check — expected status codes, no request loops, no continuous saves, no service-role credentials client-side. No RLS bypass — authenticated request-scoped Supabase access only. Loading/Empty/Error states (empty blocks, no tags/CTA, missing post, invalid/duplicate slug, save failure) — no raw Postgres stack traces. Mobile confirmation dialogs at 390 fit, readable, reachable. Minimum 14 live screenshots captured and inspected in-session (tool cannot persist image files — do not invent paths).

## 57-58. Defect Handling & Severity

Record, classify, root-cause, fix only the defect, retest, rerun regression/build. P0 (security/data integrity) = immediate FAIL. P1 (major usability/data-loss/accessibility blocker) = must fix. P2 (meaningful localized defect) = must fix before FULL PASS. P3 (minor cosmetic) = can remain documented if genuinely non-material.

## 59-66. Final Regression

`npx vitest run tests/unit/resourcesEditorR1_3.test.ts` → 19/19 PASS, no skip, no migration-gate abort. `npx vitest run tests/unit/resourcesEditorBlocks.test.ts` → PASS, record exact count. `npx vitest run --no-file-parallelism` → 0 failed tests (the final gate, not "passed individually"). `npx tsc --noEmit` → PASS. ESLint on every changed R1.3 file → PASS, no new warnings. `npm run build` → PASS, R1.3 routes present. Existing FHIP regression: do not modify/regress Authentication, Dashboard, Input Data, Investments, Insurance, Goals, Scores, DNA, Resilience, Financial Twin, Forecasting, Reports, existing Admin, R1.1, R1.2 — the serial suite + build are the principal gate. Do not reopen deferred items (scheduled publishing worker, Formula/Image/YouTube blocks, Source/Media Library, version restore, generated Supabase types, drag-and-drop, Video/Glossary/FAQ/Money Update editors, public Resources, analytics, R0-A import, AI features).

## 67. No R1.4 Work

Do not begin R1.4 — @GKTC Video, Glossary, FAQ & Money Update Management — even if closure passes. Stop after the R1.3 final report.

## 68-72. Acceptance Matrix and Verdict Rules

25/25 PASS required matrix. Functional closure checklist (25 cells, New Content/Article/Guide/Explainer/Preview usable, no overflow, block editing/move/duplicate/delete/persist, metadata usable, GREEN/AMBER/RED verified, Save/autosave/validation/unsaved-nav/revision-history verified, preview secure, sanitisation verified, keyboard/focus/labels/control-names verified, dialogs usable, console/network sane, no P0/P1/P2 open, 19/19 DB tests, block tests, serial suite zero failures, TypeScript/lint/build pass, production untouched, no R1.4 started). FULL PASS only if all conditions met. CONDITIONAL PASS only for genuinely narrow issues not affecting security/data integrity/save reliability/core workflow/editor usability/required responsive cells/keyboard accessibility (an untested cell or keyboard flow is not FULL PASS; a failing serial suite is not FULL PASS). FAIL if customer/Analyst can edit drafts, draft preview leaks, Save loses content, autosave falsely claims success, AMBER bypasses compliance, RED can publish, unsafe executable markup runs, editor unusable at a required breakpoint, keyboard cannot operate core controls, production touched, or production build fails due to R1.3.

## 73. Required Final Report

`R1.3 Closure Pass — Final Acceptance Report` with sections A (Final Verdict) through AB (Git Summary) as detailed in the full spec (Environment/Git, Authentication/Test Setup, Responsive Matrix, per-surface findings, Block Editor Functional Results table, Save/Autosave Results, Validation Results, Unsaved Navigation Results, Workflow Results, Revision History, Keyboard Accessibility, Accessibility Semantics, Responsive Defects table, Other Defects, Console/Network, State Testing, Screenshot Evidence, Code Changes, Final Regression table, Existing Application Regression, Remaining Technical Debt, Final Gate Questions, Git Summary).

## 74. Stop Gate

After producing the final acceptance report: STOP. Do not start R1.4. Wait for Product Owner review.

## 75. Execution Order

Verify Git/worktree → verify DEV environment → establish authorised live browser session → verify/use representative R1.3 fixtures → test all 5 surfaces at 5 widths → block-editor functional checks → Save/autosave checks → validation check → unsaved-navigation check → GREEN/AMBER/RED workflow → revision-history check → keyboard/focus pass → accessibility semantics → console/network → screenshots → fix only genuine defects → re-test → run DB tests → block tests → serial full suite → TypeScript → lint → stop dev server → production build → produce final report → STOP.

The goal is not new functionality. The goal is to prove that the already-built R1.3 editors are genuinely ready to become the stable editorial foundation for the next Resources phase.
