# M12E — Final DEV Certification (sections 18, 19, 20)

**Mission:** FHIP — Investment Intelligence + AIE-1, M12 "Final Closure", **Phase E — the last DEV-only phase**
**Date:** 2026-09-16 (the mission's `2026-09-15` date label is retained, as Phases B, C and D retained it)
**Branch:** `mission/m12e-final-dev-cert-2026-09-15`, branched from `356674c08ca5f27ee43df998ad611d8c8fbb4617` — M12D's closure commit, **not** `origin/main`
**Carried forward:** all 18 prior mission documents **plus** the AIE-1 requirement traceability matrix, inherited by branching (§4.1). With this document, the mission carries **19** documents plus the matrix.

**Authority exercised:** repository and git inspection; a fresh `npm install` in this worktree; full test execution; read-only PostgREST `GET` probes against DEV **and** PRODUCTION with paired negative controls; read/write against the **DEV** database for synthetic live-matrix fixtures and for the section-19 residue cleanup; a local Next dev server bound to DEV, for §20's accessibility pass.

**Authority NOT exercised, per the binding production override, and not exercised regardless of the verdict reached:** no push, no merge, no deployment, **no production database write of any kind — not one `POST`, `PATCH`, `DELETE`, RPC, DDL or DML reached the production database**, no production configuration change, no rollout change, no migration created or applied anywhere, **no secret written to any persisted config file**, and **no `.env.local` created in this worktree**.

**No credential value, API key, PAN, Aadhaar, folio number, bank number, policy number, holder name or raw statement text appears anywhere in this document or in anything it commits.**

---

## 1. Verdict

> # M12E — **CONDITIONAL PASS.**
> # M12A–E overall — **CONDITIONAL PASS.**
>
> **Everything section 18 asked to be re-run was re-run fresh, on the fully
> accumulated M12A–D code, and all of it passes.** Seven suites, 253 live-DEV and
> PGlite assertions, plus the 85-file PC4 regression contract reproducing
> **byte-identically** to M1's recorded baseline. Nothing regressed across the
> whole M12 sub-mission.
>
> **The regression baseline holds exactly: 17 files / 39 tests failing,
> file-for-file and test-for-test identical, proved twice in independent full-suite
> runs** (§3). Phase D could not measure this; it is now measured. **7,519 tests
> pass.**
>
> **Section 19 is fully closed. `OPS-PC7-2` and `OPS-M11-1` are at zero**, deleted
> by explicit id after a row-by-row safety proof, and independently re-verified by
> **M11's own probe, which this phase did not write** (§5).
>
> **Two of this phase's findings contradict documents it inherited, and both are
> reported rather than reconciled away:**
>
> 1. **M12D §8.4 states migrations `0153`/`0154`/`0155`/`0157` "remain unapplied on
>    DEV". That is wrong.** All four are applied, proved fresh against six distinct
>    schema objects with three negative controls, and corroborated independently by
>    four live suites that could not otherwise have run (§2.2). **M12A had it
>    right.**
> 2. **`aie_audit_event` held 49 rows, not the 48 every prior document records**
>    (§5.2). The extra row is a `mask_token_map_ttl_purged` system sweep event, and
>    it is **this mission's own** — generated by the very section-18 suites run to
>    certify it.
>
> **What keeps this phase, and the whole sub-mission, CONDITIONAL — and it is
> exactly five things, none of them engineering work this phase could have done:**
>
> 1. **Malware scanning is BLOCKED, not passing.** No AWS S3 path, no scanner, no
>    credentials. Re-proved fresh (§6.11). **Not claimed as a PASS anywhere.**
> 2. **`AIE_MASK_TOKEN_ENCRYPTION_KEY` is unprovisioned** — re-confirmed fresh
>    against a 9-key file. The sole cause of every M12A/M12B CONDITIONAL.
> 3. **The real-provider AI *response* path is uncertified** for all three adapters.
>    The *request* half — the half privacy turns on — is fully proved.
> 4. **Manual screen-reader testing is genuinely unavailable** here. Automated axe
>    ran on all seven PC5 states with zero violations and a live negative control.
> 5. **Traceability is 91.6% with a named basis, and only 10.5% at the strongest
>    evidence grade.** Reported as Phase D graded it, not rounded up.
>
> **`UNCONDITIONAL FULL PASS` is not available and this report does not award it.**
> §9 lists precisely what remains, and every item is a Product-Owner or operator
> action.

---

## 2. Section 18 — DEV migrations

### 2.1 Migration numbering, re-verified fresh with both controls

Not inherited. Re-derived file-level across **every ref in the repository**.

| Check | Result |
|---|---|
| In-branch guard (`scripts/check-migration-versions.mjs`) | `OK: 151 active migrations, one file per version, next version is 0158` |
| Highest version ever added on **any** ref | **`0158`** |
| **NEGATIVE CONTROL** — any version ≥ `0159` on any ref | **none** — so the method distinguishes "free" from "not looked" |
| **POSITIVE CONTROL** — the same method finds `0158` | found, on `fix/app-review-findings-2026-09-15` |
| `0156` / `0158` owner | `fix/app-review-findings-2026-09-15`, confirmed by listing that branch's tree directly |
| **Next genuinely free number** | **`0159`** |

**One methodological subtlety worth recording.** `git log --all --diff-filter=A`
lists `0156` under its **original** name `0154_app_review_0915_clear_leaked_migration_notes.sql`,
because that branch *renamed* the file rather than adding a new one. Reading the
branch's tree directly (`git ls-tree`) shows the real current state: `0156` and
`0158`. A reviewer using only the add-filter would mis-read the `0156` slot.

**A defect in this phase's own first scan is recorded rather than quietly fixed.**
The first version of the "≥ 0159" filter matched `[0-9]{4}_` anywhere in the path,
so `0154_app_review_0915_…` produced a false hit on `0915_`. Anchoring the pattern
to the basename returns the correct empty set. **A negative control that reports a
false positive is worse than none, because it looks like a finding.**

> **This phase created no migration and applied none, anywhere.**

### 2.2 `0153`/`0154`/`0155`/`0157` on DEV — and a direct contradiction of M12D

`scripts/m12e_migration_freshness_probe.mjs`, read-only `GET`, DEV only, with a
hard guard refusing to run if the DEV url equals the production url.

| Migration | Object probed | DEV |
|---|---|---|
| `0153` | table `ii_ownership_allocation` | **HTTP 200 PRESENT** |
| `0154` | table `business_entities` (CHECK widened to admit `huf`) | **HTTP 200 PRESENT** |
| `0155` | table `ii_scheme_master` | **HTTP 200 PRESENT** |
| `0157` | `ii_fund_holdings_snapshots.disclosure_period` | **HTTP 206 PRESENT** |
| `0157` | `ii_fund_holdings_snapshots.scheme_master_id` | **HTTP 206 PRESENT** |
| `0157` | `ii_fund_holdings_lines.record_checksum` | **HTTP 200 PRESENT** |
| `0157` | `ii_fund_holdings_lines.yield_pct` | **HTTP 200 PRESENT** |
| `0157` | `ii_reference_import_batches.source_column_signature` | **HTTP 200 PRESENT** |
| `0157` | `admin_users.can_view_lookthrough_data_quality` | **HTTP 206 PRESENT** |
| `0145` (positive control, applied long ago) | `aie_document_intake.fdh_bank_upload_metadata` | **HTTP 200 PRESENT** |
| **NEGATIVE CONTROL** — nonexistent table | | HTTP 404 `PGRST205` **ABSENT** |
| **NEGATIVE CONTROL** — nonexistent column, real table | | HTTP 400 `42703` **ABSENT** |
| **NEGATIVE CONTROL** — nonexistent column, `business_entities` | | HTTP 400 `42703` **ABSENT** |

> ### The discrepancy, flagged loudly rather than silently reconciled
>
> **M12D §8.4 states: *"Migrations `0153`, `0154`, `0155` and `0157` remain
> unapplied on DEV and production."* The DEV half of that sentence is wrong.**
>
> All four are applied on DEV. **M12A §8.2 probed this live and reported
> `0153`/`0155`/`0157` present on DEV**; M12D appears to have carried forward the
> older M11 §22.1 table, which correctly recorded `0157` as `PENDING` *at that
> time*, and to have restated it without re-probing — which M12D itself discloses
> it could not do, having no `node_modules` and no live run.
>
> **The production half of M12D's sentence is correct and unchanged**: all four
> remain unapplied on production, and this phase did not change that.
>
> **This is corroborated four independent ways beyond the probe**, by suites that
> could not have passed otherwise: the PC5 matrix reports `BLOCKED_ON_0153: 0`
> across 54 scenarios; the HUF matrix's `GATE-1` reports *"migration 0154 applied
> to DEV? YES — running in FULL mode"*; PC7's live run reports `0155 IS applied`;
> and PC7's `0157` columns are readable above. **A single probe could be wrong; six
> objects, three controls and four behavioural suites agreeing are not.**

**`0156` and `0158` belong to `fix/app-review-findings-2026-09-15`.** They are not
this mission's concern; this phase did not touch, re-verify or claim them.

### 2.3 The seven re-runs — all fresh, on the fully accumulated M12A–D code

| # | Suite | Command | Result | Prior result | Match |
|---|---|---|---|---|---|
| 1 | **PC7 PGlite** | `node scripts/pc7_0157_pglite_verification.mjs` | **44 PASS / 0 FAIL** | 44 | exact |
| 2 | **PC7 live DEV** | `node scripts/pc7_networth_safety_live_dev.mjs` | **19 PASS / 0 FAIL**, residue **0** | 19 | exact |
| 3 | **PC5 matrix** | `npx tsx scripts/pc5_live_dev_matrix.ts` | **54 PASS / 0 FAIL**, `BLOCKED_ON_0153: 0`, residue **0** | 54 | exact |
| 4 | **HUF matrix** | `npx tsx scripts/m4b_huf_live_dev_matrix.ts` | **15 PASS / 1 N/A / 0 FAIL**, residue **0** | 15 / 1 N/A | exact |
| 5 | **PC6 matrix** | `npx tsx scripts/pc6_live_dev_matrix.ts` | **41 PASS / 0 FAIL**, residue **0** (258 rows before = 258 after) | 41 | exact |
| 6 | **AIE II dispatch** | `AIE_M3_LIVE_DISPATCH_PROOF=1 npx vitest run --config vitest.live-dev.config.ts tests/live-dev/aieM3InvestmentDispatchLiveDev.test.ts` | **6 PASS / 0 FAIL** | 6/6 | exact |
| 7 | **PC4 invariant contract** | `npx vitest run tests/unit/ii tests/unit/reportsIIChapters tests/unit/fdh11AuInvestmentIntelligence` | **84 passed / 1 skipped / 0 failed; 1,673 passed / 5 skipped** | identical | **byte-identical to M1's baseline** |

Two further PGlite verifications were run for completeness, because §18 asks the
migration state to be re-checked rather than assumed:

| Suite | Result | Prior |
|---|---|---|
| **PC6 `0155` PGlite** | **30 PASS / 0 FAIL** | 30 |
| **HUF `0154` PGlite** | **19 PASS / 0 FAIL** | 19 |

**Total: 253 fresh live-DEV / PGlite assertions, 0 failures, plus 1,673 PC4
contract tests.** Every live suite independently re-queried its own residue to
zero.

**`GATE-2` in the HUF matrix is `NOT_APPLICABLE`, correctly, and it is worth
saying why.** That scenario exists to prove the *pre-`0154`* `entity_type` CHECK
genuinely refuses `huf` — which cannot be demonstrated once `0154` has run. It is
recorded as N/A rather than as a pass, which is the honest reading.

### 2.4 The M12A–D fixes are still fixed — spot-checked in the code, not trusted on paper

The dispatch asked for a sample. Eleven were checked directly against the
accumulated source.

| Fix | Evidence found now |
|---|---|
| `M2-OPEN-6` FDH-bank intake self-write | **no import, no call** in `app/api/aie/fdh-bank/intake/route.ts`; the 4 remaining textual occurrences are all **comments** recording the removal |
| `M12A-F1` row-extraction completeness | `RULE_ROW_EXTRACTION_COMPLETENESS` present in `reconciliation.ts:44` |
| `M12A-F2` masking-crash degradation | `orchestrator.ts:224/228` — `ai_fallback_masking_unavailable` audit + the `masking -> reconciling` edge |
| `M12B-F1` policy-number + insurance labels | `policy_number` rule present; `policy owner` / `insured person` / `life insured` / `life assured` / `proposer` all present in `piiMasking.ts:410` |
| `M12B-F3` line-crossing closed | the same rule uses `[^\S\r\n]*`, not `\s*` |
| `M3-F1` opening balance | `OPENING_BALANCE_RE.exec` at `camsParser.ts:1015`, `OPENING_BALANCE_SOURCE_REFERENCE` at `:1044` |
| `M2-OPEN-8` password limiter | `checkPasswordAttemptRateLimit` imported by **both** `documentProcessing.ts` and `payslipProcessingService.ts` |
| `CG-10` document identity | `manualImporter.ts` deterministic order + `limit(1)`, with the reasoning in place |
| `M2-OPEN-4` server-only boundary | `lib/serverOnly.ts` present and imported by all three secret-reading modules |
| `M2-OPEN-2` purge FSM | `AIE_PURGE_STATUSES` machine present in `stateMachine.ts` |
| `M2-OPEN-5` cost retry | `cumulativeInputTokens` / `cumulativeOutputTokens` hoisted outside the retry loop |

**Two of the eleven initially read as FAILURES and were wrong.** A `grep -c` on
the FDH route returned `4`, and a literal-space search for the insurance labels
returned `0`. Both were artefacts of the search, not the code — the four hits are
comments, and the labels are written with `[^\S\r\n]*` precisely because
`M12B-F3` closed the line-crossing gap. **Both are recorded because a spot-check
that reports a false failure is as damaging as one that reports a false pass**,
and the second search is the one that proves the *later* fix.

Beyond the source-level check, the behavioural proof is stronger: **M12A's and
M12B's committed certification artefacts regenerate BYTE-IDENTICALLY** when their
suites are re-run on today's code (§6.9).

---

## 3. The regression baseline — re-confirmed fresh, which Phase D could not do

**This worktree received its own `npm install`** (vitest 4.1.10, 394 packages);
`M12C-O7` / `M12B-O7`'s `node_modules` junction was not reused, and `M1-F10` /
`M12B-O7` is closed for this phase.

| Run | Result |
|---|---|
| Full suite, run 1 | 18 files / 40 tests failing |
| Full suite, run 2 | **17 files / 39 tests failing**, 7,519 passed, 18 skipped, 378 files |
| Full suite, final (after all of this phase's changes) | **17 files / 39 tests failing**, 7,519 passed, 18 skipped, 378 files |

**Runs 2 and the final run are identical at the per-file level** — every one of
the 17 files, and every per-file failing-test count, matches exactly:

```
17 adminAnalyticsPhaseAMeRoute      1 lr12rSmsfPropertyLoanLinkOverride
 1 aiResidualClosureFailClosed      1 lrFi2DebtServiceExactlyOnce
 1 countryGateAccessMatrix          9 lrFi2HouseholdDebtRatios
 1 fdh1Isolation                    8 smsfHouseholdIsolation
 + 9 `resources*` files failing at COLLECTION (0 tests each)
```

8 files × 39 tests + 9 collection-level file failures = **17 files / 39 tests.**
That reconciliation is given because the two numbers only add up once the
collection failures are counted as files and not as tests.

**Run 1's single extra failure is identified precisely, not waved past.** It was
`m12aFdhBankAccuracyCorpus.test.ts > FDH-A01`, and the message is
`Error: Test timed out in 5000ms` — a **timeout, not an assertion**. It is the
same flake class M12C documented for three other files under concurrent load.
**Independently confirmed:** running that suite alone on a cold process reproduces
the timeout; re-running it warm gives **15 passed / 15**. FDH-A01 is the corpus's
first case, so it absorbs module load and JIT cost. **No assertion in that suite
ever failed.**

> **M12C's warning about self-regenerating evidence files was vindicated, on this
> phase, exactly as written.** The cold run that timed out **rewrote**
> `scripts/m12a-fdh-bank-certification/results.json` (+397 lines) and
> `results_table.md` with the degraded result. Both were restored with
> `git checkout` and re-verified byte-identical, and the suite re-run green. **A
> certification artefact regenerated by a suite is only trustworthy when that
> suite actually passed** — checking `git diff` after every run is the cheap
> habit that catches it.

`npx tsc --noEmit`: **clean.** `npx eslint` on every changed and new file:
**clean.**

---

## 4. Carried-forward state

### 4.1 The 18 prior documents and the traceability matrix

Inherited by branching from `356674c`, so they are the same git objects and
**cannot** have drifted. Proved, rather than asserted, by the strongest available
check:

```
git diff --stat 356674c08ca5f27ee43df998ad611d8c8fbb4617 HEAD -- docs/
   (no output — the entire docs/ tree is byte-identical)
```

The 18 mission documents in `docs/investment-intelligence/`:
`AIE_II_DISPATCH_CLOSURE_REPORT_2026-09-15.md`,
`AIE_II_POST_PC4_FINAL_CERTIFICATION_2026-09-15.md`,
`AIE_INFRA_CLOSURE_REPORT_2026-09-15.md`, `AIE1_TERMINAL_CERTIFICATION_2026-09-15.md`,
`II_PC4_MIGRATION_AND_CONFIG_BASELINE_2026-09-15.md`,
`II_PC4_POST_CLOSURE_REGRESSION_CONTRACT_2026-09-15.md`,
`II_PC4_VERIFICATION_VERDICT_2026-09-15.md`,
`II_POST_PC4_MASTER_SCOPE_LEDGER_2026-09-15.md`, `PC5_HUF_ADDENDUM_2026-09-15.md`,
`PC5_IMPLEMENTATION_CERTIFICATION_2026-09-15.md`,
`PC6_MARKET_DATA_CERTIFICATION_2026-09-15.md`, `PC6_OPERATOR_RUNBOOK.md`,
`PC7_LOOKTHROUGH_CERTIFICATION_2026-09-15.md`,
`PC8_PC9_PC10_SCOPE_CLOSURE_2026-09-15.md`, `M12A_FDH_BANK_CLOSURE_2026-09-15.md`,
`M12B_INSURANCE_PRIVACY_CLOSURE_2026-09-15.md`,
`M12C_INTEGRITY_HARDENING_CLOSURE_2026-09-15.md`,
`M12C_PC4_OWNER_MAPPING_OPERATOR_ACTION.md`, `M12D_TRACEABILITY_CLOSURE_2026-09-15.md`
— that listing is **19 entries**, because M12C's operator sheet is a distinct
document that M12C's own count folded into its total of 17. Either way, **every
file M12D carried is present and byte-identical.**

Plus the matrix: `docs/aie-programme/AIE_1_REQUIREMENT_TRACEABILITY_FINAL_2026-09-15.md`
(3,859 lines), likewise byte-identical.

With this document: **19 mission documents in `docs/investment-intelligence/`
plus the matrix.**

### 4.2 Production and repository state

| Check | Result |
|---|---|
| `origin/main` | **`23b49da1d63c9c20f980ea9042e176845b6f60e3`** — unchanged, as in every prior phase |
| Pushed | **nothing.** `git rev-parse @{u}` → *no upstream configured for branch `mission/m12e-final-dev-cert-2026-09-15`* |
| Merged | **nothing** |
| Production database — **writes** | **zero.** Every production contact is a `GET`, and both are row counts used as the §5 safety control |
| Production configuration / deployment / rollout | **untouched** |
| Migrations created or applied | **0** |
| Secrets written to any persisted config | **0.** No `.env.local` exists in this worktree |
| Outbound AI provider calls | **0** |

---

## 5. Section 19 — DEV residue cleanup

### 5.1 The safety verification, done before anything was deleted

`scripts/m12e_dev_residue_safety_probe.mjs` — read-only, `GET` only. The standard
applied is deliberately stricter than *"it is on DEV"*: every row had to be shown
**structurally inert** (it can carry no economic meaning) **and unreferenced by
anything that survives**. Rows failing either test would be reported NOT SAFE and
left alone.

**`OPS-PC7-2` — `ii_fund_holdings_snapshots`, 19 rows:**

| Property | Result |
|---|---|
| Rows with **zero** constituent lines | **19 / 19** — and `ii_fund_holdings_lines` holds **0 rows in the entire table** |
| `source_id` NULL | 19 / 19 |
| `scheme_master_id` NULL | 19 / 19 |
| `batch_id` NULL | 19 / 19 |
| No `source_url` and no `source_sha256` | 19 / 19 |
| `disclosure_period`, `amc_name`, `line_count`, `resolved_weight_total_pct` | **NULL on all 19** |
| `created_at` | **all 19 on 2026-08-24** — the R5 era, three weeks before this mission began |
| `source_document_version` | **18 × `vc-doc-1`, 1 × `mr-doc-1`** |

**A correction to the inherited record.** Every prior document (M7's
`PC7_LOOKTHROUGH_CERTIFICATION`, M11 §21, `PC8_PC9_PC10_SCOPE_CLOSURE`) describes
these as `source_document_version='vc-doc-1'`. **One of the 19 is `mr-doc-1`.**
It is identical in every safety-relevant respect — zero lines, NULL source,
same creation date — so it does not change the verdict, but the inherited
description was not exactly right and is corrected here.

That every PC7-era column is NULL is itself corroboration: these rows **predate
`0157` entirely**, which is precisely what an R5 fixture header should look like.

**`OPS-M11-1` — `aie_audit_event`:**

| Property | Result |
|---|---|
| Total rows | **49** (see §5.2) |
| Live `aie_document_intake` rows | **0** |
| Live `aie_extraction_run` rows | **0** |
| Distinct `intake_id` referenced | 13 |
| …still present in `aie_document_intake` | **0** — every one is an orphan |
| Rows with no `user_id` (system sweep) | 8, all `mask_token_map_ttl_purged` |
| `created_at` range | 2026-09-12 … 2026-09-15 |

Because the entire AIE pipeline holds **zero** intake and **zero** run rows on
DEV, **no audit event can have a surviving parent**. That is the whole set, not a
sample.

**PRODUCTION CONTROL, read-only `GET`:** `ii_fund_holdings_snapshots` = **0
rows**, `aie_audit_event` = **0 rows**. There is no production equivalent of
either set, so this cleanup has no production analogue and can never be confused
for one.

### 5.2 The 49th row — `aie_audit_event` was 48 in every prior document

This phase's **first** probe of the session, before any suite had been run,
reported **48** — matching M11 exactly. The safety probe, run after the
section-18 suites, reported **49**.

The delta is **+1 `mask_token_map_ttl_purged` row with no `user_id`** (M11
recorded 7 of these; there are now 8). It was generated by **this phase's own
section-18 live-DEV runs** — most likely the AIE II dispatch proof, which
installs an ephemeral masking key and exercises the TTL sweep.

**This is worth stating plainly: certifying the system generates residue in the
system being certified.** The row was not pre-existing contamination and it is
not a defect; it is the observation cost of the proof. It is disclosed because
every prior document says 48, and a reader finding 49 should not have to wonder
which is wrong. **It is why the cleanup was deliberately sequenced AFTER every
live-DEV run in §18 and §20, not before.**

### 5.3 The cleanup

`scripts/m12e_dev_residue_cleanup.mjs`, DEV only, guarded by an explicit project-ref
assertion and a refusal if the DEV url equals the production url. Four disciplines,
each because the alternative is a real failure mode:

1. **The safety predicate is re-evaluated INSIDE the cleanup script**, immediately
   before deletion — never inherited from the earlier probe, because rows can
   change between two runs.
2. **Rows are deleted BY EXPLICIT ID**, never by a blanket predicate, so a row that
   appeared between check and delete cannot be swept up.
3. **Any row failing the predicate is skipped and reported**, not deleted.
4. **The tables are independently re-queried afterwards** with a separate request —
   a delete returning success is not treated as proof.

A **dry run** was executed first and reported `19 deletable / 0 skipped` and
`49 deletable / 0 skipped` without touching anything.

| Set | Before | Deleted by explicit id | Skipped | After (independent re-query) |
|---|---:|---:|---:|---:|
| `OPS-PC7-2` — `ii_fund_holdings_snapshots` | **19** | **19** | 0 | **0** |
| `OPS-M11-1` — `aie_audit_event` | **49** | **49** | 0 | **0** |

**ANTI-VACUITY CONTROL.** The identical counting method still reports non-zero
where rows genuinely exist and were deliberately **not** touched:
`ii_prices_nav` = 258, `ii_benchmarks` = 14, `ii_fund_holdings` (R1 shape) = 32.
**So the zeros mean "empty", not "the query was broken".**

### 5.4 Independent re-verification, by a probe this phase did not write

Re-run afterwards with **M11's own** `scripts/m11_dev_residue_probe.mjs`,
unmodified:

```
aie_document_intake 0   aie_extraction_run 0   aie_field_candidate 0
aie_parser_attempt 0    aie_unresolved_item 0  aie_reconciliation_run 0
aie_review_decision 0   aie_audit_event 0      aie_mask_token_map 0
aie_masking_summary 0   aie_ai_completion_attempt 0  aie_ai_cost_attempt 0
ii_fund_holdings_snapshots 0   ii_fund_holdings_lines 0
snapshots with source_id NULL  0     ii_ownership_allocation 0
```

> **`OPS-PC7-2` and `OPS-M11-1` are CLOSED.** M11's probe was written to report
> `OPS-PC7-2` as *"the named non-zero exception"*. It now reports zero.

**Nothing else was deleted.** Only the two named sets, only by id, only after the
per-row proof above.

---

## 6. Section 20 — full DEV certification

Every line below is a **fresh** run on the fully accumulated code. Where a prior
phase proved the same thing, both results are given.

| # | Item | Required | Fresh result | Prior result | Status |
|---|---|---|---|---|---|
| 1 | **PC4 invariant pack** | PASS | 84 passed / 1 skipped / 0 failed; **1,673 passed / 5 skipped** | identical (M1, M12A, M12B) | **PASS** — byte-identical |
| 2 | **PC4 reduced synthetic pack** (M12C) | PASS | **11 / 11** cases (S00–S10), residue **0** | 11/11 (M12C) | **PASS** |
| 3 | **AIE unit** | PASS | **74 files / 1,222 tests, 0 failures** (51 `aie*` + M12A/B/C + PC5/6/7 suites) | — | **PASS** |
| 4 | **AIE live** | PASS | **6 / 6** — `aieM3InvestmentDispatchLiveDev` | 6/6 (M3) | **PASS** |
| 5 | **PC5** | PASS | **54 / 54**, `BLOCKED_ON_0153: 0`, residue 0 | 54/54 | **PASS** |
| 6 | **HUF** | PASS | **15 PASS / 1 N/A / 0 FAIL**, residue 0; PGlite **19 / 19** | 15/1 N/A | **PASS** |
| 7 | **PC6** | PASS | **41 / 41**, residue 0; PGlite **30 / 30** | 41/41 | **PASS** |
| 8 | **PC7** | PASS | live DEV **19 / 19**, residue 0; PGlite **44 / 44** | 19, 44 | **PASS** |
| 9 | **FDH AIE** (M12A) | PASS | corpus 15/15; `results.json` **byte-identical** — 100.00% field P/R, 100.00% econ P, 92.50% econ R, **0** unexplained omissions, **0.00%** false accept, **0.00%** false canonical write | identical | **PASS** (FDH-A05 conditional, §6.10) |
| 10 | **Insurance AIE** (M12B) | PASS | corpus suite green; `results.json` **byte-identical** — 100.00% field P/R, 100.00% econ P, 82.76% econ R, **0** unexplained omissions, **0.00%** false accept, **0.00%** false canonical write | identical | **PASS** (INS-B05/B13 conditional, §6.10) |
| 11 | **Malware** | *not claimable* | **BLOCKED** — §6.11 | blocked | **BLOCKED — NOT a PASS** |
| 12 | **Privacy** | PASS | `m12bAiPrivacyProof` green; `privacy_proof.json` **byte-identical** — 3/3 egress cases PASS, financial evidence survived 5/5, 3/3, 2/2 | identical | **PASS**; real-provider **response** half **BLOCKED** |
| 13 | **Prompt injection** | PASS | FDH-A10 and INS-B08 both reproduce byte-identically: every figure extracted as printed, the injected `999999.99` never any field's value, terminal status unchanged, **zero** AI calls | identical | **PASS** |
| 14 | **Cost** | PASS | `m12cCostRetryAccounting` + `aieCostAdmissionPglitePostgresProof` (**6/6**, real Postgres) green | 17/17, 6/6 | **PASS** |
| 15 | **Purge** | PASS | `aiePurgeService` + `m12cAiePurgeStatusContract` green; PC5 S-45 proved a real storage purge **independently verified absent** on DEV | green | **PASS** |
| 16 | **Password** (M12C) | PASS | `m12cPasswordLimiterAndDocumentIdentity` green | 17/17 | **PASS** |
| 17 | **Document identity** (M12C) | PASS | same suite, §11 block green | green | **PASS** |
| 18 | **Accessibility** | PASS except screen reader | **39 passed / 0 failed**, all **7** states, **0** violations each, negative control fired, residue 0 | 7 states, 0 violations | **PASS**, screen reader **NOT AVAILABLE** |
| 19 | **Traceability** | report Phase D's own figures | **2,091 / 2,284 = 91.6%** with a real named basis; **only 239 = 10.5%** at grade A; 193 = 8.4% honestly UNVERIFIED; **0 orphans** | — | **CONDITIONAL, as graded** |
| 20 | **DEV residue** | 0 | **0** — both sets closed and independently re-verified | 19 + 48 outstanding | **PASS** |

### 6.9 Why "byte-identical evidence" is the strongest form of items 9, 10, 12 and 13

M12A's and M12B's certification artefacts are **regenerated by their own suites**.
Re-running those suites today, on code that has since absorbed every M12C fix —
the parser change, the masking change, the FSM audit, the cost accumulator, the
address rule — and getting `git diff --exit-code` clean on
`scripts/m12a-fdh-bank-certification/` and `scripts/m12b-insurance-certification/`
means **every accuracy, privacy and injection metric reproduces exactly**. That is
a stronger statement than "the tests still pass": it says the *numbers* did not
move.

### 6.10 What items 9, 10 and 12 still do NOT certify

Unchanged from M12A §6.5 and M12B §5.6, re-confirmed fresh here:
**`AIE_MASK_TOKEN_ENCRYPTION_KEY` is UNSET** — asserted in-process this phase, and
absent from the shared 9-key `.env.local` (names inspected; no value read, printed
or copied). FDH-A05, INS-B05 and INS-B13 have their deterministic invariants proved
under an ephemeral in-process key; **the AI clarification itself is NOT CERTIFIED**
for any adapter, and no real provider response has ever been obtained.

### 6.11 Malware — BLOCKED, stated precisely, not claimed as a pass

Re-verified fresh:

- **No AWS SDK.** `aws-sdk` / `@aws-sdk` appear **zero** times in `package.json`.
- **No S3 code path.** Every match for `S3Client` / `PutObjectCommand` / `GuardDuty`
  in `lib/` and `app/` is a **comment or a type declaration** — four files, all
  disclosure text.
- **Zero production callers.** Nothing in `lib/` or `app/` imports
  `lib/aie/malware/**`. The decision function is tested in isolation and has no
  scanner behind it and nobody calling it.
- The file's own header says so: it exists *"so the decision logic"* is ready,
  with no *"GuardDuty scan to ever run against yet"*.

> **There is no malware scanning in this system today.** `PO-BLOCKER-1` / `OA-2a` /
> `OA-2b` are unresolved: the exact bucket name, the AWS account id and a working
> IAM policy are all still outstanding, and the runbook
> (`aie-document-quarantine-dev`) and the IAM policy (`fhip-aie-quarantine-dev`)
> still disagree on the name. **Credentials alone would not be sufficient — the
> application has no S3 code path to use them.** This is reported as BLOCKED and
> is **not** counted as a PASS anywhere in this report.

### 6.12 Accessibility — what was actually measured, and what was not

`scripts/m12c_pc5_accessibility_live_dev.ts` was re-run against a **real Next dev
server** bound to DEV, with `AIE_REVIEW_PC5_PROJECTION_ENABLED=true`.

All **seven** states reached and scanned with `@axe-core/playwright`
(`wcag2a` + `wcag2aa`): resolution centre (24 rules passed), owner choice (26),
blocking-unresolved (26), joint allocation (26), duplicate decision (26),
correction overlay (26), resolved state (24). **Zero violations on every one.**
All six keyboard/live-region items PASS, driven with real key presses and real
assertions. **Harness negative control fired**: injected broken markup produced
violations, and they disappeared when it was removed — the scanner is
demonstrably capable of failing. Zero residue, independently re-queried.

**One `needs-review` result per state** — `color-contrast`, `M12C-O12` — is
reported as incomplete, folded into neither column.

**The first attempt reached only 4 of 7 states** and died on a 30-second
Playwright navigation timeout while the dev server compiled a route (it logged
*"Slow filesystem detected"*). The same dynamic route had already been navigated
successfully earlier in that very run, so this was a transient compile stall, not
a defect; the re-run on a warm server completed all seven. **Recorded rather than
silently retried.**

> **Manual screen-reader testing was NOT performed and is NOT claimed.** No NVDA,
> JAWS, VoiceOver or Orca is installed or drivable here. The ARIA live-region
> behaviour was verified *programmatically* — roles, `aria-live`, and rendered
> text — which is **not** the same as verifying what a screen reader announces.

### 6.13 Traceability — reported as Phase D graded it

| | |
|---|---:|
| Requirements in the Product Owner's specifications | **2,284** |
| Classified with a real, named, checkable basis | **2,091 — 91.6%** |
| Honestly `UNVERIFIED` (no artifact exists; none invented) | **193 — 8.4%** |
| Orphan / unaccounted for | **0** |
| **Grade A** — id cited at a specific `file:line`, artifact addresses the subject | **239 — 10.5%** |
| Grade B — a named artifact implements the family's subject; this member not separately re-proved | 1,681 — 73.6% |
| Grade C — resolved by a governed decision, deferral, prohibition or exclusion | 171 — 7.5% |
| Grade D — `UNVERIFIED` | 193 — 8.4% |

**These are not rounded up and the grading nuance is not simplified away.**
91.6% is the share with a *named basis*. **10.5% is the share proven at the
requirement level.** Grade B means *"the subject is implemented and the artifact
is named"*, not *"this requirement was individually certified"*. Phase D also
discloses that **38 of the 239 grade-A rows are citations Phase D itself added**;
a reader who regards a self-added citation as weaker should read 201 A / 1,719 B.

**This phase did not re-derive the matrix** and makes no independent claim about
it beyond confirming it is present and byte-identical.

---

## 7. What changed on this branch

| File | Change |
|---|---|
| `docs/investment-intelligence/M12E_FINAL_DEV_CERTIFICATION_2026-09-15.md` | **new** — this document |
| `scripts/m12e_migration_freshness_probe.mjs` | **new** — §2.2's read-only DEV probe with three negative controls |
| `scripts/m12e_dev_residue_safety_probe.mjs` | **new** — §5.1's read-only row-by-row safety proof |
| `scripts/m12e_dev_residue_cleanup.mjs` | **new** — §5.3's guarded, dry-runnable, delete-by-id cleanup |
| `scripts/m12e_dev_server_launcher.mjs` | **new** — starts the dev server for §6.12 **without writing a `.env.local`** |
| `scripts/m12e-dev-residue/**` | **new** — the committed safety verification, cleanup result and log |
| `scripts/pc5_live_dev_matrix.ts` · `scripts/m4b_huf_live_dev_matrix.ts` | **env resolution only** — a shared-checkout `.env.local` fallback, the identical two-candidate pattern `pc6_live_dev_matrix.ts` and `pc7_networth_safety_live_dev.mjs` already use. **No assertion, scenario or logic changed** |
| `scripts/pc5-live-dev-results.json` · `pc6-live-dev-results.json` · `pc6-0155-pglite-results.json` · `pc7-0157-pglite-results.json` · `pc7-networth-safety-live-dev-results.json` · `m12c-pc4-synthetic-pack/**` · `m12c-pc5-accessibility/**` | Regenerated by this phase's own fresh runs — **this phase's evidence** |

**Source logic changed: none.** **Migrations: none.** **Production contact:
read-only `GET` row counts only.**

Two files were deliberately **reverted, not committed**:
`scripts/ii-r5-certification/comparison_report.json` and
`scripts/ii-r6p1-certification/comparison_report.json`. The only line that changed
in either is `generatedAt`; **every certification figure reproduced
byte-identically** — the same incidental corroboration M1, M2, M3, M12A and M12B
each recorded.

---

## 8. Open items

Nothing new was found by this phase that changes any prior verdict. New
observations first, then the inherited set re-confirmed rather than restated from
memory.

| Id | Item | Owner | Status |
|---|---|---|---|
| **`M12E-O1`** *(new)* | **M12D §8.4 mis-states DEV migration status.** `0153`/`0154`/`0155`/`0157` **are** applied on DEV; M12D says they are not | documentation | **Corrected here** (§2.2). M12D's production half is right |
| **`M12E-O2`** *(new)* | **Prior documents record `aie_audit_event` = 48; it was 49.** The extra row was generated by this mission's own section-18 runs | disclosed | **Closed by §5.3** — the table is now 0 |
| **`M12E-O3`** *(new, minor)* | **One of the 19 `OPS-PC7-2` headers is `mr-doc-1`, not `vc-doc-1`** as every prior document states | disclosed | **Closed** — safety-identical, deleted with the rest |
| **`M12E-O4`** *(new, minor)* | **`m12aFdhBankAccuracyCorpus` FDH-A01 times out at 5s on a cold process**, and a failing run **rewrites** the committed `results.json` | engineering, trivial | A per-case timeout on that suite would remove the flake. Passes 15/15 warm |
| **`OA-11`** *(inherited, unchanged)* | Rebuild II data from a clean re-import to close PC4's 4 remaining residuals — **deletes real production rows** | **Product Owner** | **NOT this phase's to resolve, and not resolved** |
| **`OA-6`** *(inherited, re-confirmed fresh)* | `AIE_MASK_TOKEN_ENCRYPTION_KEY` unset — absent from a 9-key file and from the process environment | PO / operator | Open. The sole cause of every M12A/M12B CONDITIONAL |
| **`PO-BLOCKER-1` / `OA-2a` / `OA-2b`** *(inherited, re-proved fresh)* | **AWS S3 + GuardDuty**: no SDK, no code path, no credentials, runbook and IAM policy still disagree on the bucket name | PO / operator + engineering | Open. §6.11 |
| **ADRs** *(inherited from M12D)* | The 18 `ADR-AIE-*` artifacts required by AIE-1.0 do not exist | **Product Owner** | Open — write them, or formally retire the requirement |
| **`M12B-O2` / `M12C-O6`** *(inherited)* | The real-provider **response** path is uncertified for all adapters | PO | Open. Needs key provisioning plus authorisation for outbound spend |
| **`OA-8` / `M12C-O8` / `M12C-O9`** *(inherited)* | PC5 owner mapping — `household_members` empty, no screen creates one, the two exception stores are unbridged | PO / engineering | Open. M12C's operator sheet is ready |
| **`OA-1`** *(inherited)* | PC4's ≥48-section specification has never been found | PO | Open |
| **`M12C-F4` / `M12C-F5` / `M12C-O1`–`O5`, `O10`–`O12`, `M12B-O1`, `O3`–`O6`, `M12A-O1`–`O4`** | Disclosed defects and limits, each out of its phase's scope | various | **Unchanged.** None was re-opened or silently closed by this phase |
| **`M12C-O7` / `M12B-O7`** *(inherited)* | Worktree `node_modules` junction | housekeeping | **CLOSED for this phase** — a real `npm install` was performed (§3) |

---

## 9. What remains before UNCONDITIONAL FULL PASS

Most of the engineering is done. **Five things remain, and not one of them is
work an autonomous DEV-only phase may perform.**

1. **AWS S3 + GuardDuty — real implementation and real credentials.** The exact
   bucket name, the AWS account id, a working IAM policy, reconciliation of the
   runbook/policy name disagreement — **and then the S3 code path itself, which
   does not exist**. Until then malware scanning is BLOCKED, not failing.
2. **`AIE_MASK_TOKEN_ENCRYPTION_KEY` provisioning, then the real-provider proof.**
   The key unblocks the masking stage; an authorised outbound call is then needed
   to certify the provider **response** path. Both need the Product Owner: one is a
   secret, the other is spend against their own credential.
3. **The `OA-11` production-data decision.** PC4's four remaining residuals have a
   proved mechanism (`M12C-F1`: production's `ii_transactions` is a union across
   five parse runs, three of them failed, and the fixed run contributed none of
   them). The remediation **deletes real user rows** and is a genuine
   Product-Owner decision, not an engineering task.
4. **The 18 `ADR-AIE-*` records — written, or formally retired.** Several of the
   decisions were made and implemented anyway; documenting settled facts and
   retiring the obligation are both defensible. It is a decision, not engineering.
5. **The human-present merge / production track itself.** Nothing in this mission
   has ever been pushed, merged or deployed. `origin/main` is still `23b49da`, and
   migrations `0153`/`0154`/`0155`/`0157` remain unapplied on production.

Two smaller items would close the last named gaps but do not block a full pass on
their own: **manual screen-reader testing** (needs an environment with a real
screen reader) and **traceability's grade-B majority** (needs per-requirement
re-certification, not more documentation).

---

## 10. For the human-present session

1. **This is the end of the DEV-only work.** Sections 18, 19 and 20 are complete.
   Every remaining item in §9 requires a person with credentials, spend authority,
   production access, or the authority to make a product decision. **The next step
   is a session with the Product Owner present — not another autonomous dispatch.**

2. **Do not read "the fix shipped" as "the data is right".** `M12C-F1` is the
   single most important thing this sub-mission learned, and it still holds:
   production's transaction set is a frozen union across five parse runs, and no
   later run can repair it because every later run deduplicates against the
   contamination. **`OA-11` must be executed before any claim about production II
   data is safe.**

3. **Check `git diff` after every suite run.** This phase reproduced M12C's
   warning exactly: one flaky cold-start timeout silently rewrote a committed
   certification artefact with a degraded result (§3). It was caught and restored
   because the diff was checked. **A self-regenerating evidence file is only
   trustworthy when the run that wrote it passed.**

4. **Re-probe, do not restate.** Two inherited documents were wrong in this
   phase's own subject area — M12D on DEV migration status, and every prior
   document on the `aie_audit_event` count. Both were found by running a query
   rather than by re-reading a table. **The cheapest error in this programme is
   carrying forward a number nobody re-measured.**

5. **Certifying this system adds rows to it.** §5.2's 49th audit event is small
   and harmless, but it is the reason the residue cleanup was sequenced last. Any
   future live-DEV campaign should expect the same and clean up **after** itself,
   not before.

6. **Applying the migrations to production is a four-step ordered sequence and
   `0157` requires `0155` beneath it**: `0153`, `0154`, `0155`, then `0157`.
   `0156` and `0158` belong to `fix/app-review-findings-2026-09-15` and **must not
   be applied as part of this mission's sequence**.

---

*End of M12E closure report, and of the M12 DEV-only sub-mission. Nothing is
pushed. Nothing is merged. No production database write, configuration change,
migration, deployment or rollout was performed, no secret was provisioned in any
persisted config file, and no `.env.local` was created in this worktree.*
