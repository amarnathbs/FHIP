# AIE-1 Final Closure — Independent Verification (2026-09-13)

**Verifies:** `docs/aie-programme/AIE_1_FINAL_CLOSURE_CERTIFICATION.md` (branch `feature/aie-1-final-closure`, HEAD `c27b21d`, base `7331c19` as originally reported).
**Method:** direct source inspection of every claim below, plus independent re-execution of the mission's own live-DEV/live-HTTP verification scripts against real DEV Supabase and a real running dev server for this exact worktree — not a re-read of the background agent's own report. Where a script produced results, they are reproduced verbatim below, not paraphrased from the original report.
**Verdict on the report's reliability: the report is accurate.** No claim checked below was found to be inflated, fabricated, or contradicted by direct evidence. Several claims that could not be independently re-run (real OpenAI/AWS calls) were confirmed BLOCKED for the same reason the report gives — no credentials present in this environment.

---

## What was independently re-checked, and how

| Claim | Independent check performed | Result |
|---|---|---|
| Closure migrations renumbered 0147/0148 → 0149/0150 to avoid colliding with `origin/main`'s own new `0147` | Listed `supabase/migrations/` on this branch directly; confirmed no `0147`/`0148` file exists here, `origin/main` has a different `0147` (the G6-G8 archive-bypass fix from this same session's other workstream) | **Confirmed** |
| Migration `0129` comment-only divergence from `origin/main`, pre-existing not introduced by this closure | `git diff` of `0129` between this branch and `origin/main` | **Confirmed** — the only diff is an appended comment block (G8.056 operational warning), zero behavioral change |
| OpenAI provider pins the exact snapshot `gpt-4o-mini-2024-07-18`, never the floating alias, in the real API call | Read `lib/aie/provider/openaiAieProvider.ts` and `lib/aie/config.ts` in full | **Confirmed** — `getAieAiModel()` defaults to the pinned snapshot; the request body sends `req.model` (sourced from that function) |
| No hardcoded API key; separate env var from Module 11's own OpenAI key; fails loud (never silently substitutes mock) when `openai` explicitly selected without a key | Read `openaiAieProvider.ts` + `providerFactory.ts` in full | **Confirmed** — `AIE_OPENAI_API_KEY` (distinct name), throws `ProviderError('AUTH', ...)` / a named `Error` respectively, no fallback path exists in either file |
| `store: false` sent explicitly, never described as Zero Data Retention | Read the request body construction | **Confirmed**, and the code comment makes the same distinction the report does |
| Real OpenAI call never made — blocked by missing `AIE_OPENAI_API_KEY` | Grepped `.env.local` for `AIE_OPENAI_API_KEY`/`AWS_ACCESS_KEY`/`AWS_SECRET` | **Confirmed absent** — 0 matches, consistent with the disclosed BLOCKED status |
| Purge/temporary-document lifecycle wired into all 3 real intake routes (immediate-delete for Insurance/generic, `accept.ts`-time for II/FDH-bank) | Grepped every caller of `finalizeDocumentBinaryAfterRun` across `app/` and `lib/` | **Confirmed** — called directly in `app/api/aie/intake/route.ts` and `app/api/aie/insurance/intake/route.ts`; called from `lib/aie/review/accept.ts` at both the Investment Intelligence (line 394) and FDH-bank (line 446) success branches, matching the claimed reason (those two need the original bytes at accept time) |
| Purge service never marks `deleted`/`purged` without independently re-verifying the storage object absent first | Read `lib/aie/services/purge.ts` in full | **Confirmed** — every success path calls `verifyQuarantineObjectAbsent` before any DB status transition; a delete-call success alone is never treated as sufficient |
| GuardDuty scan-result decision logic (`decideScanResult`) fails closed on every named security property (account/region/bucket/key binding, stale-version rejection, threats-found unconditional block, unrecognised-enum fail-closed, idempotent duplicate handling, deadline expiry) | Read `lib/aie/malware/scanResultHandler.ts` in full, traced every branch | **Confirmed** — every property the report claims is enforced is present in the code exactly as described; no path admits extraction except the single `COMPLETED` + `NO_THREATS_FOUND` branch, and every identity/version field is checked independently before that |
| GuardDuty logic NOT wired into a live pipeline (storage is still Supabase, not S3) | Grepped for any caller of `decideScanResult` outside its own test file | **Confirmed** — no production caller exists |
| Cost admission (migration `0150`) is genuinely atomic (no read-then-write race) | Read the full migration SQL | **Confirmed** — `aie_reserve_ai_cost` does the allowance check and the increment in one `UPDATE ... WHERE (reserved+settled+amount) <= allowance ... RETURNING`, which Postgres executes as a single atomic statement; `SECURITY DEFINER` + `search_path = public` set correctly; RLS enabled with zero policies, execute grants restricted to `service_role` only |
| Pilot cohort gate wired into all 3 real intake routes, never into accept/purge-sweep | Grepped for `PilotCohort`/`pilotCohort` across `app/api/aie` | **Confirmed** — present in `intake/route.ts`, `insurance/intake/route.ts`, `fdh-bank/intake/route.ts`; absent from accept/purge-sweep routes, matching the claimed "disabling uploads should not block cleanup" rule |
| No HTTP intake route exists anywhere for Investment Intelligence | Listed every file under `app/api/aie/**/route.ts` | **Confirmed** — only `intake`, `fdh-bank/intake`, `insurance/intake`, `review/*`, `unresolved-items`, `cron/purge-sweep`, `intake/[intakeId]` exist; no `investment-intelligence` route anywhere |
| PC5 does not exist anywhere in this codebase as a real module | Grepped case-insensitively for `pc5` across `lib`/`app` outside the new interface/test files | **Confirmed** — the only other hits are a reconciliation-code naming reference, a type, and one feature flag (`isAiePc5ProjectionEnabled`) whose own comment says "PC5 projection is NOT built this pass" |
| `tsc --noEmit` clean | Ran it myself, full repo | **Confirmed clean**, zero output |
| `eslint` clean on every touched non-test source file (82 files) | Ran `eslint` myself against the exact file list from `git diff --name-only origin/main...HEAD` (excluding test files) | **Confirmed clean**, zero errors/warnings |
| AIE-scoped suite: 462/462 tests pass | Ran `npx vitest run tests/unit/aie*` myself | **Precisely: 461/462 passed in the combined run; the one failing test (a 5-second timeout in `aieInsuranceIntakeRouteRegistersAdapter.test.ts`) passed when re-run in isolation.** Resource contention under a 42-file concurrent run is the plausible explanation (consistent with the report's own disclosed flaky-test caveat for a different file), but an isolated pass alone does not itself prove that cause — it rules out "this assertion is wrong," not "why it timed out." Stated as fact only to the extent shown: not a reproducible failure, cause not conclusively established. |
| `@axe-core/playwright` genuinely added as a new dependency (not just referenced) | Checked `package.json` | **Confirmed** present at `^4.13.0` |
| PC5 interface: 16/16 real live-DEV checks incl. genuine 6-way concurrent idempotent collapse, zero residue | **Independently re-ran `scripts/aiecl_pc5_interface_live_dev_check.ts` against real DEV myself** | **16/16 PASS, zero residue — reproduced exactly** |
| Insurance regression: 14/14 real live-DEV/live-HTTP checks, zero regression from this session's changes, incl. the disclosed migration-0149-blocked DB-bookkeeping gap | **Independently re-ran `scripts/aiecl_insurance_regression_live_dev.ts` against a real running dev server for this worktree and real DEV myself** | **14/14 PASS, zero residue — reproduced exactly**, including the same disclosed gap (storage object genuinely deleted; DB status update blocked because migration 0149 isn't applied yet — this is a real, live-observed consequence of the DDL-application blocker, not a code defect) |
| Accessibility: real axe-core scan, 2 UI states, zero violations, zero residue | **Independently re-ran `scripts/aiecl_accessibility_live_dev_check.ts` against the same dev server myself** | **8/8 PASS, zero violations, zero residue — reproduced exactly** |
| Migration state check is read-only, confirms `aie_*` tables exist and are queryable on both DEV and production | **Independently re-ran `scripts/aiecl_closure_migration_state_check.mjs` myself** | **Reproduced exactly** — every `aie_*` table exists on both projects, all row counts 0 (no residue anywhere), correctly cannot distinguish "0146 applied" from "0140-0145 applied, 0146 not yet" for production (a pure trigger-function replacement has no new queryable object) — the report's own honest disclosure of this specific unresolved question holds |
| No production writes or migration application occurred; no push, no merge | `git log`/`git status` on the worktree; the branch remains local-only, `git diff --stat origin/main...HEAD` shows only new files/tests, no migration file was applied via any script this session ran (all scripts used pre-existing tables) | **Confirmed as stated here — corrected wording.** The original phrasing ("no DEV write") was imprecise: this closure mission's own live-DEV scripts (PC5, Insurance regression, accessibility) DID write real rows to real DEV — synthetic users, intake/run/item/policy rows — as an inherent, disclosed part of how they prove behavior. Every one of those writes was independently confirmed cleaned up (zero residue, re-verified after cleanup, not merely asserted). The accurate statement is: **no production writes or migration application, and no push/merge — but real, disclosed, fully-cleaned-up temporary DEV test writes did occur**, which is exactly what "real-DEV verification" is supposed to mean. |

## What could not be independently re-verified (and why — matches the report's own disclosure)

- **Real OpenAI API call / real observed cost** — `AIE_OPENAI_API_KEY` is genuinely absent from this environment (confirmed by grep), so no real call could be made by me either. The report's 12 contract tests (mocked `fetch`) are real and were re-run as part of the 462-test suite above; they are correctly described as not standing in for real-provider verification.
- **Real GuardDuty scan / real S3 provisioning** — no AWS credentials in this environment, confirmed by the same grep. Not independently re-checkable from this session's access level either.
- **Migration 0149/0150 DB-side application** — this session has no DDL execution channel (same constraint disclosed in the report); this was not challenged, and the Insurance regression re-run above independently reproduced the exact consequence of that gap (storage delete works, DB bookkeeping doesn't) rather than merely trusting the claim.
- **Migration 0146 genuinely applied to production specifically** (vs. 0140-0145 only) — re-ran the same read-only probe; it is structurally incapable of distinguishing the two states, exactly as the report says. **Precise status: previously reported verified in DEV (a real behavioral reproduction, per `AIE_1_0146_INSURANCE_CLOSURE_REPORT.md`); its presence on production specifically is not reconfirmed by this table-existence probe** (a trigger-function replacement has no new queryable object for a schema probe to see). This probe's inconclusiveness does not cast doubt on, and does not invalidate, the earlier DEV verification — it is a distinct, narrower, still-open question about production only. **This is not grounds to reapply migration 0146 anywhere** — reapplying a migration to resolve an evidence gap, rather than a confirmed defect, is not warranted.
- **Full 6707/6734-test whole-repository regression figure** — not re-run in full during this verification pass (time-bounded); the AIE-scoped subset (the part actually touched by this closure mission) was independently re-run in full instead, which is the higher-value check for regression risk specifically introduced by this branch.

## Overall assessment

This is the most heavily self-disclosed and most independently reproducible closure report seen in this session's history of background-agent work: every claim that could be re-run against real infrastructure (DEV Supabase, a real running dev server) was re-run by me, from a cold start, using the mission's own scripts, and produced **identical pass/fail results** to what the report claims — including reproducing a genuinely disclosed gap (the migration-0149-blocked DB bookkeeping) rather than a clean success being papered over. Every claim about code behavior (fail-loud provider selection, fail-closed GuardDuty logic, genuine SQL-level atomicity, correct route wiring, absence of routes/modules) was independently confirmed by direct source reading, not relayed.

**I am treating this report's claims as verified and reliable.** The phase verdicts in the original report (all CONDITIONAL PASS, explicitly not FULL PASS, explicitly not production-authorized) are consistent with the evidence and are not being upgraded or downgraded by this verification — they were accurate as stated.

**Not pushed, not merged to `main` — unchanged by this verification pass.** No new authority is granted by this document; it verifies, it does not advance, the branch's status.
