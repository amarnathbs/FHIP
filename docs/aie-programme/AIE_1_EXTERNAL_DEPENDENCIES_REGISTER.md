# AIE-1 — External Dependencies Register

Tracks items AIE-1's own engineering cannot close by itself — each depends on
something owned outside this workstream (another programme, an operator
action, or a credential/subscription decision only the Product Owner can
make). Distinguished from AIE's own remaining implementation work
(tracked in `AIE_1_FINAL_CLOSURE_CERTIFICATION.md` §9/§14) precisely so a
future reader does not confuse "AIE hasn't built this yet" with "AIE cannot
build this without someone else acting first."

| # | Dependency | Owned by | Current state (confirmed this session, not assumed) | What unblocks it |
|---|---|---|---|---|
| 1 | **PC5 (exception-consumption downstream module)** | Investment Intelligence programme's own roadmap (`docs/investment-intelligence/II_PC4_STATUS_2026_09_07.md`'s PC4→PC5→PC6→PC7 sequence) — not AIE's to build | Confirmed: no PC5 table, route, service, or module exists anywhere in this repository (exhaustive grep). AIE's own consumer-side interface (`lib/aie/pc5/pc5ExceptionInterface.ts`) is built and real-DEV verified (16/16, re-confirmed independently twice this session) — it is the AIE half of an integration whose other half does not exist yet. `isAieReviewPc5ProjectionEnabled()` exists with zero consumer behind it. | Investment Intelligence programme reaching and building its own PC5 phase. Not an AIE-1 task, not on AIE-1's critical path, and not something this workstream should attempt to build a placeholder for (that would violate the master plan's own "no second exception system" rule — see the interface report's own §1). |
| 2 | **`AIE_OPENAI_API_KEY`** | Product Owner / whoever holds the OpenAI account and billing | Confirmed absent from `.env.local` this session (0 matches). No account, project, or billing relationship for this key is visible to this session. | The PO (or whoever manages the org's OpenAI account) creates a project-scoped API key for AIE specifically (deliberately separate from Module 11's own `OPENAI_API_KEY`, per the mission's own "don't let one feature's outage/quota affect an unrelated feature" rule) and shares it through a secrets channel — never pasted in chat (see this session's own established credential-handling procedure). This session can then set it in DEV's env and run the first real, masked, cost-observed OpenAI call. |
| 3 | **AWS S3 quarantine + GuardDuty Malware Protection provisioning** | AWS account owner (this session found real AWS CLI credentials configured, `arn:aws:iam::879807128139:user/Amar`, but confirmed by direct testing this session that this identity has **zero permissions** for every relevant read/write action tried: `amplify:ListApps`, `cloudfront:ListDistributions`, `iam:ListAttachedUserPolicies`/`GetUser`, `s3:ListAllMyBuckets` — all `AccessDenied`. GuardDuty specifically returned `SubscriptionRequiredException`, meaning **GuardDuty is not even enabled/subscribed on this AWS account at all** — this is an account-level, ongoing-cost decision, not a permissions gap) | GuardDuty Malware Protection for S3 carries real, ongoing per-scan AWS cost once subscribed; an S3 bucket is a new, persistent, billable resource. Neither exists today. | An explicit Product Owner decision to (a) subscribe the AWS account to GuardDuty Malware Protection for S3 (real recurring cost — needs cost acceptance, not just a permissions grant) and (b) grant this session's IAM identity (or a new, purpose-scoped one) least-privilege permissions to create exactly the one S3 bucket + GuardDuty protection plan + EventBridge/SQS/DLQ + IAM roles documented in `AIE_1_CLOSURE_AWS_INFRASTRUCTURE.md`'s ready-to-run runbook. This session will not provision billable, persistent cloud infrastructure or subscribe an AWS account to a new paid service without that explicit go-ahead, regardless of what a written mission authorizes in the abstract — this is a hard-to-reverse, cost-bearing, outward-facing action. |
| 4 | **Migrations `0149`/`0150` — CLOSED 2026-09-13.** ~~Document-lifecycle purge bookkeeping and atomic AI cost admission~~ | ~~Product Owner~~ **Applied by the PO to DEV this session.** | **Both confirmed live and verified**: `aie_document_intake.purge_status`/`purge_due_at`/`purged_at` columns exist; the Insurance regression journey re-run end-to-end shows the full immediate-deletion path (storage delete AND DB bookkeeping) now working, not just the storage half; the 24-hour hard-retention backstop was live-DEV proven for the first time (16/16, aged row correctly swept, fresh negative control correctly untouched, real storage objects). **A real bug was found in `aie_reserve_ai_cost` by live-testing it immediately after 0150 landed** — see item 5 below, now the only open migration item. | Closed. See item 5 for the one follow-on fix this discovery required. |
| 5 | **Migration `0151` — `aie_reserve_ai_cost` single-row-contract fix** | Product Owner, same channel | Written and committed (`be5af50`), **not yet applied**. Root cause: PL/pgSQL's `RETURN QUERY` does not exit a function; the original 0150 body fell through past its success branch into denial-path code, returning 2 rows (a correct `true` row plus a spurious contradictory `false` row) on every successful reservation. Confirmed live via direct RPC calls, both a success case and a denial case. **Current real-world impact is contained, not severe**: the TypeScript caller reads `data[0]` (the correct row, emitted first) and the ledger's actual balance/attempt counters are not double-counted — verified live. But the function violates its own declared single-row `returns table(...)` contract, and any future `.single()`-based caller would throw. | PO applies `supabase/migrations/0151_aie1_cost_admission_reserve_single_row_fix.sql` to DEV. Not urgent/blocking (current integration works correctly today), but should land before any new caller is added to this RPC. |

## What this register is not

This is not a list of things AIE-1 "gave up on." Every dependency above has
a real, implementation-complete, unit-tested AIE-side counterpart already
built and, where a real DEV database column doesn't block it, already
live-DEV verified. The register exists so a future reader — or a future
session picking up this work — does not mistake "waiting on someone else"
for "not done" or attempt to route around a genuine credential/cost/subscription
decision that is not this workstream's to make unilaterally.
