# FDH-4 — Test Certification

Consolidated regression evidence for this dispatch. R7's own certification pack (`docs/r7-bank-csv-engine/R7_160_CASE_CERTIFICATION.md`, `R7_TESTING_AND_VERIFICATION.md`) is reused and extended, not rewritten (spec section 81).

## Environment

- Canonical main starting point: `origin/main` @ `71e68f8` (R7 merge commit) — confirmed via `git fetch` + `git log`, matches the task's stated tip exactly.
- Branch: `feature/fdh-4-bank-csv-integration`, created off `71e68f8`.
- Fresh `npm install` (491 packages) — the shared `node_modules` junction was absent in this worktree.
- `core.autocrlf false` confirmed.

## Migration guards

```
node scripts/check-migration-versions.mjs
  → OK: 66 active migrations, one file per version, next version is 0067.
node scripts/check-migration-versions-against-branch.mjs origin/main
  → OK: no cross-branch migration collisions between HEAD and origin/main.
```

Migration `0066_fdh4_bank_adapter_coverage_expansion.sql` is pure additive seed data (no DDL beyond `insert`/`update`) — drafted per this environment's constraint (no DDL-execution credential), delivered as a file for manual application, never claimed applied.

## Independent oracle (spec sections 82-83)

```
npx tsx scripts/r7_oracle_compare.ts
  → R7 independent oracle comparison — 10 fixtures
  → Total comparisons: 327
  → Total failures: 0
```
All 10 fixtures (6 R7-original + 4 FDH-4-new) cross-checked field-by-field (row counts, date, amount, direction, `description_clean`, reference, `balance_after`) against a Python oracle that imports nothing from the production TypeScript — genuinely independent expected values (spec section 83), not generated by the code under test.

## New certification cases

```
npx vitest run tests/unit/fdh4AdapterCoverage.test.ts
  → 20/20 passed
```
Breakdown: 4 detection-positive cases, 2 registry/versioning cases, 10 cross-adapter false-positive negative controls (spec section 60 — proving e.g. an ANZ file never detects as CBA/NAB/Westpac/Macquarie and vice versa), 4 reconciliation cases including one deliberate negative control (a 0.01 balance alteration that must produce `failed`, not `reconciled` — spec section 84's "alter one transaction by 0.01" requirement, applied to the new adapters specifically).

## Full regression

```
npx vitest run
  → Test Files  101 passed | 1 skipped (102)
  →      Tests  1958 passed | 5 skipped (1963)
```
Includes, unmodified and still passing: `r7AccountIdentity` (13), `r7CertificationAndAdversarial` (23), `r7CsvIntake` (26), `r7Deduplication` (28), `r7Detection` (20), `r7LargeFile` (999/1000/1001/2500/5001/10000-row cases), `r7Normalization` (24), `r7Pagination` (6), `r7Reconciliation` (25), `r7SchemaContract` (22) — R7's full pack, zero regressions from the widened adapter registry. Also unmodified and passing: FDH-1, FDH-2, FDH-3 suites, Investment Intelligence (R1-R6) suites, Resources suites.

```
npx tsc --noEmit -p tsconfig.json
  → clean, 0 errors
npm run build
  → succeeds, 0 compile errors, all routes generated (static + dynamic)
```

## Lint

```
npx eslint lib/financial-data-hub/bank-csv tests/unit/fdh4AdapterCoverage.test.ts
  scripts/fdh4_live_dev_certification.ts scripts/fdh4_live_scale_certification.ts
  → 0 errors, 0 warnings

npx eslint . --quiet   (full repo, errors only)
  → 6 pre-existing errors, all in files FDH-4 did not touch
    (components/grid/FinancialDataGrid.tsx, components/recommendations/
    RecommendationsPanel.tsx, components/ui/AppShell.tsx)
  → confirmed pre-existing via `git stash` + re-lint against the
    unmodified canonical-main tree: same 6 errors present before any
    FDH-4 change
```
**No new lint regression.** FDH-4's own new/changed files are 100% clean.

## Live DEV (see `FDH4_SECURITY_CERTIFICATION.md`, `FDH4_SCALE_CERTIFICATION.md` for full transcripts)

```
npx tsx scripts/fdh4_live_dev_certification.ts   → 13/13 PASS
npx tsx scripts/fdh4_live_scale_certification.ts → 4/4 PASS (row integrity,
                                                     DB retrieval, reconciliation, cleanup)
```

## Consolidated count

```
Vitest (local/PGlite):        1958 / 1958 passed  (5 pre-existing skips, unrelated)
Independent oracle:            327 /  327 field comparisons, 0 discrepancies
Live DEV pipeline+security:     13 /   13 checks
Live DEV 10,000-row scale:       4 /    4 checks
TypeScript:                    clean
ESLint (FDH-4 files):          clean
Production build:              clean
Migration guards (local + cross-branch): both clean
```
