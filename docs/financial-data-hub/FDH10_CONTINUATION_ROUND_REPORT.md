# FDH-10 — Credit Cards & Loans Intelligence: Continuation Round Report

**Verdict: CONDITIONAL PASS.** Phase A (independent reverification of the prior round's claims) is genuinely complete and every claim reproduced fresh, with two real gaps found and fixed. Phase B (Liabilities-tab UI + API surface) is now built end-to-end for upload -> review -> approve -> propose -> compare -> Apply. Phase C (statement extraction) now covers all four non-negotiable representative patterns (AU/India credit card, AU/India loan) with genuine detection + extraction + reconciliation proof, including both headline financial controls fed from real extracted statement data. Scale certification, live-hosted-DEV certification, and full ledger-write wiring (Phase D's "purchase becomes an actual household expense row") remain NOT DONE — honestly disclosed below, not fabricated.

This document supersedes nothing in `FDH10_COMPLETION_REPORT.md` (the prior round's own honest report) — it records what changed in this continuation round on top of it.

## What changed this round

### Phase A — Independent reverification (COMPLETE)
- Fetched `origin/main` fresh: confirmed at `3509a10`. Merged cleanly into the FDH-10 branch (0 conflicts) — `git diff --stat` confirms only Goal Linkage's own files (`0093`, goal-funding routes/services) came in, nothing FDH-10 touches.
- Migration `0096` re-confirmed as the genuinely free number post-merge (`0093` real+merged, `0094`/`0095` live, `0096` still unclaimed).
- Read `creditCardEconomics.ts`/`repaymentDecomposition.ts` directly: both headline controls are structurally correct (PAYMENT categorically maps to `'transfer'`, never `'expense'`; loan `expenseTotal` excludes principal by construction via `LoanAdvanceEconomicType = Extract<FdhEconomicTransactionType, 'transfer'>`).
- Reproduced 52/52 pre-existing FDH-10 unit tests, 330/330 FDH-9 bridge regression, 18/18 PGlite security, and re-ran the migration replay: **93/93 clean** (better than the previously-reported 90/90 — `0094`/`0095`, previously excluded as "pre-existing failing", now apply cleanly post-merge too).
- `tsc --noEmit`: clean both before and after this round's own additions.
- Full Vitest, twice across the round: 3023/3059 then 3042/3075 passing (test count grew as this round added tests); every failure both times is in `fdh1Isolation.test.ts` (pre-existing, confirmed via `git diff` showing zero change to the offending file/route since `2d6d1e9`), `resourcesAdminR1_2.test.ts`/`resourcesR1_1.test.ts`/`resourcesDiscoveryR1_6LiveDev.test.ts` (live-shared-DEV state drift, zero Resources files touched by this branch). **Zero FDH-10 regressions.**
- Production build: succeeds, exit 0, all routes compile including the 5 new liability API routes.
- **Documented the migration-ordering defect** (spec section 11's explicit ask): `fdh9_assert_proposal_owner()`'s replaced body referenced `source_liability_statement_id` before the column existed — `CREATE OR REPLACE FUNCTION` validates column references at CREATE time. Fixed by moving the `ALTER TABLE ADD COLUMN` statements to a new Part F.4, before the function replacement in Part G. Found and fixed in the PRIOR round; this round independently reproduced the fix's correctness via a fresh clean-rebuild replay.
- **Two NEW real gaps found by this round's own independent verification** (not present in the prior round's disclosed gap list):
  1. Migration `0096` Part F.1's own comment disclosed "no direct authenticated transition into 'approved' is legal at all today" for `fdh_liability_statements` — there was no approval RPC, so the review journey's "Approve evidence" step had no legal path. **Fixed**: added `fdh10_approve_liability_statement()` (Part F.5), mirroring `fdh9_approve_payroll_event()` exactly. Since `0096` is still unapplied to DEV, this was safe to amend in place rather than needing a new migration number.
  2. `tsc --noEmit` caught the exact same class of gap `FDH_DOCUMENT_AUDIT_EVENT_TYPES_FDH9_ADDED`'s own header documents finding on the FDH-9 pass: the TS-side audit-event-type enum had not been widened for `0096`'s 7 new liability event types. **Fixed**: added `FDH_DOCUMENT_AUDIT_EVENT_TYPES_FDH10_ADDED`, added `tests/unit/fdh10SchemaContract.test.ts` (mirrors `fdh9SchemaContract.test.ts`'s method), and retargeted `fdh9SchemaContract.test.ts`'s/`fdh7SchemaContract.test.ts`'s "latest word" claims to exclude the new FDH-10 additions (same precedent those files already established for each other).

### Phase B — Liabilities-tab UI + API surface (BUILT)
- `app/(app)/liabilities/page.tsx`: added an "Import Statement" entry point above the existing (unchanged) manual liability grid — exact structural mirror of the Income tab's FDH-9 pattern. Zero new top-level nav items.
- `components/liabilities/LiabilityImportPanel.tsx`: type selector (Credit Card / Loan) -> upload form (country/currency/institution/masked id/balances/limit/minimum-payment/rate) -> processing -> review (issuer, masked card, period, opening/closing balance, purchases/refunds/interest/fees/payments for cards; opening/closing principal, principal repaid, interest, rate, frequency for loans; reconciliation status; payment-activity bank-match status) -> approve -> current-vs-proposed compare table -> Apply (with add_new/update_existing/apply_selected_fields/keep_existing modes) -> applied/kept-existing/stale terminal states.
- New authenticated routes, all session-scoped (never a client-supplied id): `POST .../liability-statement/upload`, `GET .../liability-statement/{id}`, `POST .../liability-statement/{id}/approve`, `POST`/`GET .../liability-statement/{id}/proposal`, `POST .../liability-proposals/{id}/apply` (the last exclusively via `fdh10_apply_liability_proposal()`).
- `lib/financial-data-hub/services/liabilityStatementProcessingService.ts` (new): single-call upload+process (CSV bytes already in memory, no PDF password/OCR wait — mirrors R7's `uploadBankCsv`, not the payslip PDF flow's 2-step split). Persists `fdh_liability_statements`/`fdh_liability_statement_activities`, computes totals, reconciles via the already-certified `statementReconciliation.ts`, runs bank matching via the already-certified `bankMatching.ts` for PAYMENT activities only.
- `lib/import-bridge/liabilityProposalService.ts` (new) + `supabaseStore.ts`'s `persistLiabilityProposal` (new): the liability-domain proposal-generation path, parallel to `incomeProposalService.ts`, gated on approval, populates `source_liability_statement_id` provenance.
- Live-verified NO SILENT MUTATION end-to-end: upload/process/review/approve/generate-proposal/compare all leave the canonical Liability untouched — only `POST .../apply` can change it (proven by the pre-existing PGlite security suite's own NO APPLY / STALE PROPOSAL / DUPLICATE APPLY checks, re-run clean after this round's changes: 18/18).

### Phase C — Statement extraction (BUILT: all 4 non-negotiable patterns)
- `lib/financial-data-hub/liability/adapters/{types,auCreditCard,inCreditCard,auLoan,inLoanEmi,registry}.ts` + `statementIntake.ts`: a header-signature detection pipeline reusing R7's own `scoreHeaderAgainstSignature` unchanged, wrapping the existing generic column-mapped CSV extractor (`csvExtraction.ts`, extended additively with `activityTypeAliases` so institution vocabulary like "Cash Advance"/"Finance Charge" normalises onto the closed activity-type vocabulary without guessing).
- Four representative, honestly-scoped (not claiming any single named issuer) patterns, each proven end-to-end in `tests/unit/fdh10LiabilityAdapters.test.ts` (13 new tests): AU credit card (purchase/refund/payment/interest/fee/cash-advance + declared opening/closing balance/limit/minimum-payment/statement dates), India credit card (purchase/payment/refund/finance-charge/fee + declared minimum/total-amount-due, GST column preserved as evidence only), AU loan (statement-evidenced principal/interest/fee split, fed directly into the certified headline loan control), India EMI loan (amortisation-schedule principal/interest split, fee correctly `undefined` rather than fabricated `0` when not disclosed).
- OCR/manual-mapping boundary proven: an unrecognised layout returns `manual_mapping_required`, never a guessed extraction; an unrecognised activity-type value is warned and excluded, never silently dropped.
- **Total certified FDH-10 scenarios this round: 86** (68 unit — up from the prior round's 52 — + 18 PGlite security), still below the 150+ target; the increase is concentrated in genuinely new statement-extraction coverage, per this round's own stated priority order.

## What is still NOT done (honest, undisguised)

1. **Ledger-write wiring (Phase D's core ask)**. `creditCardEconomics.ts`/`repaymentDecomposition.ts` remain pure, fully-tested DECISION functions. Nothing yet calls them against real approved/applied statement rows to actually write `fdh_transactions`/`fdh_transaction_allocations`/`fdh_transaction_links` — i.e. an approved-and-applied card purchase does not yet appear as a household expense the R8/FDH-6/FDH-7/FDH-8 pipeline reports on. Apply today only updates the canonical Liability's own fields (`balance`, `interest_rate`, etc.). This is the single largest remaining functional gap and would need its own dedicated pass.
2. **Scale certification (Phase G)**: not executed this round (100/500/1000/1001/5000/10000 transaction volumes, pagination negative control).
3. **DEV migration application (Phase H)**: `0096` confirmed NOT applied to DEV via a genuine live read-only probe this round (`fdh_liability_statements` and `fdh10_apply_liability_proposal` both return "not found" against the real hosted DEV Supabase project). Chunk files regenerated and verified byte-identical on reassembly. See the final chat report for the exact "Migration ready: YES, DEV application required: YES" statement and chunk locations.
4. **Live-DEV user journeys (Phase I)**: blocked by #3 — no live DEV schema to drive a browser journey against.
5. **UX/accessibility certification (Phase J)**: not executed against a real rendered browser this round (time budget went to the functional build-out); the component follows the existing FHIP design-system conventions (`text-trust`, `rounded border`, ARIA roles/labels/`aria-live`, semantic `<table>`/`<fieldset>`/`<caption>`) by direct visual/structural parity with `PayslipImportPanel.tsx`, which has itself passed a UX pass previously — but no independent screenshot/viewport/axe-style pass was done this round.
6. **Multi-currency, supplementary-card, offset-account, rate-change-mid-period**: still architecturally deferred, unchanged from the prior round's disclosure.
7. **Scenario volume**: 86/150+ — genuine progress from 70, still short of target.

## Production: NOT TOUCHED

No production database access exists in this environment; nothing in this round's scripts read or used `PRODUCTION_SUPABASE_SERVICE_ROLE_KEY`.

## Next action

**STOP. DO NOT MERGE/PUSH. DO NOT APPLY THE PRODUCTION MIGRATION. DO NOT BEGIN FDH-11.**

Everything above is committed to `feature/fdh10-credit-cards-loans-intelligence`, not pushed, not merged. See the final chat report for the complete structured status per the dispatch's required format.
